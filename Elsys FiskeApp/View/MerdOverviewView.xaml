<UserControl x:Class="Elsys_FiskeApp.View.MerdOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Elsys_FiskeApp.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource InfoPanelBackgroundBrush}" Width="{Binding WindowWidth}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Overview"
               Grid.Row="0" Grid.Column="0"
               FontSize="28" 
               FontWeight="Bold"
               Foreground="{StaticResource LightTextBrush}"
               VerticalAlignment="Center"
               />

        <Button x:Name="toggleBar"
            Grid.Row="0" Grid.Column="1"
            Content=">"
            Width="30" Height="30"
            Margin="10,0,0,0"
            Command="{Binding ToggleMenuBar}"
            VerticalAlignment="Center"
            />

        <ScrollViewer Grid.Row="1" Grid.ColumnSpan="2">
            <ItemsControl ItemsSource="{Binding MerdViews}" Padding="20">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10" CornerRadius="5" BorderThickness="5"
                            BorderBrush="{StaticResource ButtonBackgroundBrush}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="{Binding MerdName}"/>
                                <TextBlock Grid.Row="1" Text="{Binding ConnectionStatus}"/>
                                <TextBlock Grid.Row="2" Text="{Binding WellbeingStatus}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>

</UserControl>
